# Migration

Training code currently lives at `../../../text-ip-adapter/`. This experiment intentionally did not symlink that code; it will be copied here when the prototype is refactored onto `core/`.

# Core

Shared framework placeholders for prompt-adapter research: encoders turn references into latent summaries, projectors map latents into model-compatible spaces, injection defines where latents enter frozen models, training holds reusable loops, eval holds shared measurements, and data holds loaders and dataset contracts.

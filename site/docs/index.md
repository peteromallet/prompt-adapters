# Prompt Adapters

Prompt Adapters studies reference-conditioning adapters that inject encoded-reference latents into frozen base models across text, audio, music, and video.

The experiment grid is TODO and will be populated by `tools/build-site.sh`, filtered by `consequential=true` with a toggle for minor experiments.
